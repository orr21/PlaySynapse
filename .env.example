# --- CREDENCIALES DE ALMACENAMIENTO (MinIO / S3) ---
# Usuario y contraseña para acceder a la consola de MinIO (http://localhost:9001)
# Estas mismas credenciales se usarán automáticamente para el acceso tipo S3 (AWS_ACCESS_KEY_ID)
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=password123
MINIO_ENDPOINT=http://minio:9000

# --- INTELIGENCIA ARTIFICIAL ---
# API Key para Groq (necesaria para el narrador de IA en nba_live_app)
# Consíguela en https://console.groq.com/keys
GROQ_API_KEY=gsk_tu_api_key_aqui

# --- CONFIGURACIÓN DE REDPANDA (KAFKA) ---
# No suele ser necesario cambiar esto en docker interno, pero se deja por si acaso.
REDPANDA_SERVER=redpanda:9092
